node_modules/
dist/
.DS_Store
*.log
*.tsbuildinfo
banks/_promoted/
banks/banks-raw/
banks/case_sources/
scratch/
# Blind-scoring answer key: retain locally, never publish in the public repository.
audit/stage-reference-semantic-census-2026-07-23/checker/hidden-calibration-key.jsonl
# Generated TTS artifacts (deterministic, regenerable from banks/*.json; the
# queue manifest is ~25 MB and the opus blobs ship out-of-band — see DECISIONS 20).
audio/manifest.queue.json
audio/*.opus
