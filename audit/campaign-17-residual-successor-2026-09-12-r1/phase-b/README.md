# Phase B — legacy-primary representation survey

Terminal: `CAMPAIGN17_LEGACY_PRIMARY_SURVEY_READY`.

The live strict population is **75 missingRequiredAnchor rows across 19 parents in three banks**, separate from 66 revealsAllStages rows. All 75 satisfy the bounded mechanical migration predicate. There are **zero semantic exceptions to representation equivalence**. This does not certify that any existing clinical content or chosen historical boundary is sound.

The proposed operation is exactly one field addition per row: `answerableAfterStageId = stageId`, preserving the original string and retaining `stageId`. `patch-plan.json` contains every identity, exact JSON-pointer path, absent-field precondition, value, bank/parent/part hashes, ordered topology, before/after resolution, visible/hidden stage IDs and visible-payload hash. `parents.json` preserves all 19 complete source parents. `semantic-holds.json` is empty. **No canonical bank was written.**

The proof uses the live code, not a blanket stage rule:

1. `src/caseVisibilityBoundary.ts` resolves an absent primary through a resolving legacy string. Adding that exact string makes the primary resolve at the same inclusive index. The resolver's diagnostic `field` changes, but its selected prefix does not.
2. `src/examLayout.ts:getVisibleCaseStages` uses that index to return the inclusive prefix. Every candidate's complete visible stage payload is equal before/after. The survey also compares every other staged part across all banks after the proposed clone mutation. Global titles/exhibits and every learner/scoring value are unchanged.
3. `src/types.ts` permits string anchors. `src/schema.ts` accepts nonempty strings, rejects duplicate stage IDs and malformed baseline objects, and places both ordinary anchor fields under the same unfolding-metadata floor. Membership is separately audited. The three affected banks already declare schema `2.1`; no version change or typed-baseline substitution is proposed. `src/bankImport.ts` detects either ordinary field in the same feature branch. `src/allowedKeys.ts` allows both fields.
4. `scripts/audit/audit-stage-refs.ts` defines this strict finding as absent primary plus resolving legacy. Before: 66 leakage findings + 75 legacy-primary findings. After the in-memory proposal: the exact same 66 leakage findings and no legacy-primary findings. Default finding payload/output is unchanged. A legacy reference to the last stage legitimately returns the full prefix; this is still a resolved reference, not fail-open leakage.
5. Removing only the 75 added properties from the proposed clones yields deep equality with every original bank, including keys, metadata, legacy values, ordering, parent/part populations, bilingual text and visual payloads. Whole-bank validation passes before/after. Canonical file bytes and Campaign 16 hashes remain unchanged on disk.

Fourteen controls demonstrate the boundary of the claim: present primary values (including null, stale string and typed baseline), absent/stale legacy, duplicate/reordered topology, dropped plan, changed target value, changed learner content, deleted legacy/part, a different resolving primary, and malformed primary objects. Existing primaries are never overwritten. The different-primary control confirms that copying an arbitrary stage value would change behavior; exact equality is essential.

The focused executable is `../tools/legacy_survey.ts`, invoked through `../tools/run.py`; its receipt exits **0**. The actual canonical strict CLI exits **1**, reporting 0 unresolved, 66 revealsAllStages and 75 missingRequiredAnchor. That expected live failure is preserved, not reclassified as a pass. Phase A's full canonical checks remain applicable because their bank/code/census inputs have not changed. No established gate, default candidate policy, fatality rule or normal output was modified.

Next: a qualifying producer-independent reviewer verifies the predicate, hashes, exact operations and controls. An authorized integration seat may then implement the reviewed plan with source-precondition checks, normal review/gates, census-drift procedure and accepted-work history publication. Any newly requested clinical/boundary correction belongs in a separate semantic plan. The current producer cannot independently approve its own proof, and this survey supplies no direct mutation authority.
