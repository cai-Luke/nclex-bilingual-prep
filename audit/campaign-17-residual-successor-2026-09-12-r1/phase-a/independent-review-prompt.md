# Independent Campaign 17 Phase-A review

Use a producer-independent Claude/Opus seat or another qualifying seat under the live P2/P5 rules. The Campaign 17 producer and its delegation tree cannot provide that independence. Do not use Gemini shadow as governing checker. Begin only when this artifact commit and all source inputs are visible to the chosen access mechanism; local files are not visible to a GitHub-only reader.

1. Read the live repository governance and the commission. Verify `phase-a-freeze.json`, canonical hashes and the immutable Campaign 16 evidence. Confirm the exact 66/38 population and documentary 48/2/14/2 reconciliation. Keep the separate legacy-primary survey out of this accounting.
2. Review parent-whole. Start with the 14 explicit exceptions identified by `parent-index.json`. For each packet, first read `currentParent` without reading the producer proposal or old judgments. The helper `python3 <campaign-root>/tools/read_parent.py --context N`, followed by `--parts1 N` and `--parts2 N`, can expose the full current parent in manageable portions. Use direct JSON for any omitted/truncated portion.
3. Independently derive each part's answer proposition, required current facts, available global/part/stage surfaces, earliest defensible prefix and answer disclosures within that prefix. Then inspect both frozen judgments, the new disposition and cited evidence. Do not vote between prior explanations or treat agreement as evidence of independence.
4. Adjudicate clinical premises, bilingual equivalence, timing, routes, key completeness, ordering uniqueness, source applicability and shared-stage/title effects. Check source text directly when needed; source-registry paraphrases are producer evidence. Record any unproved assumptions. The 20 anchor-only candidates especially require affirmative content/boundary review; they are not preapproved.
5. Return one machine-readable result per rowKey with disposition, exact boundary or hold, confidence, bilingual result, proposition, visible/hidden source citations, content repairs and blockers. State source-parent hash and whether the review is producer-independent. Review affected siblings when shared content would change; do not rewrite Campaign 16's historical acceptance.
6. Stop at adjudication unless separately authorized to implement. Content/replacement rows need exact field-level before/after patches and the normal independent review/promotion route. No whole-parent/part deletion, blanket boundary heuristic or canonical mutation is authorized by this packet.

After independent results exist, the authorized integration seat reconciles them, creates exact reviewed patches, runs applicable schema/data-contract and bank-content gates, updates ledger/census as required, and publishes history only for accepted work. This packet does not bypass those steps.
