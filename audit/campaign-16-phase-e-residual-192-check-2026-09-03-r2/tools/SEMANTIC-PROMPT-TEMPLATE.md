You are independently checking whether full-stage visibility in a bilingual nursing case materially gives away the answer to an active part. Return only JSONL: one JSON object per dispatched target and no prose or fences.

For each target determine the exact tested decision; which visible stage facts are required; whether an otherwise unnecessary stage fact directly reveals or strongly cues the keyed condition, action, priority, calculation, trend, response, outcome, ordering, or distractor elimination; whether the case intentionally uses the complete record; whether an unsafe boundary requires REVIEW; and whether the relation is parallel in English and Simplified Chinese. A later timestamp or extra datum is not automatically a leak. NO_LEAK_COMPLETE_RECORD requires positive complete-record intent. Use the stored key and rationale only to identify the intended inference, not to re-grade clinical truth.

Verdict is exactly one of LEAK, NO_LEAK_COMPLETE_RECORD, NO_LEAK_NONANSWERING_DATA, REVIEW. Bilingual relation is exactly one of PARALLEL, EN_ONLY_LEAK, ZH_ONLY_LEAK, MATERIAL_DIVERGENCE, UNRESOLVED. Ordinary compression, punctuation, or sentence-count differences are not material divergence.

Emit exactly this shape and no other top-level field:

{"rowToken":"r_...","verdict":"LEAK | NO_LEAK_COMPLETE_RECORD | NO_LEAK_NONANSWERING_DATA | REVIEW","testedDecision":"one item-specific sentence, no more than 30 words","stageEvaluations":{"s_first_literal_token":{"contribution":"REQUIRED | UNSAFE | NONANSWERING","evidenceIds":["e_..."],"basis":"one item-specific sentence"},"s_second_literal_token":{"contribution":"REQUIRED | UNSAFE | NONANSWERING","evidenceIds":["e_..."],"basis":"one item-specific sentence"}},"partEvidenceIds":["e_..."],"stageEvidenceIds":["e_..."],"locus":{"stageToken":"s_... or null","evidenceIds":["e_..."],"verbatimSpan":"minimal exact packet text or null"},"bilingualRelation":"PARALLEL | EN_ONLY_LEAK | ZH_ONLY_LEAK | MATERIAL_DIVERGENCE | UNRESOLVED","reason":"1-3 item-specific sentences"}

The packet supplies each target's declaredStageTokens. Copy those literal tokens as the keys of stageEvaluations: every declared token exactly once, no missing token, no added token. stageEvaluations is a keyed object, never an array. The skeleton above illustrates explicit literal slots; create exactly the slots named by that target. You are not asked to reproduce stage order. Object-key order and JSONL row order do not matter.

Every stage gets an explicit judgment. REQUIRED means needed to answer; UNSAFE means leakage evidence; otherwise NONANSWERING. REQUIRED and UNSAFE need nonempty evidenceIds valid for that stage. NONANSWERING may have none, but cite a concrete fact you discuss. Every contribution must be supplied; never use null or omit it. Each basis must be item-specific. Placeholder, filler, template-completion, and housekeeping text are rejected.

LEAK exists iff at least one stage is UNSAFE. For LEAK, locus.stageToken names an UNSAFE key, locus.evidenceIds is nonempty and stage-owned, and each locus ID appears in stageEvidenceIds. For non-LEAK, no stage is UNSAFE, locus.stageToken is null, locus.evidenceIds is empty, and locus.verbatimSpan is null.

partEvidenceIds contains 1-4 target-owned IDs. stageEvidenceIds contains 1-8 stage-owned IDs for declared stages, selecting the strongest plausible exposed cues even for no-leak judgments; it need not equal the per-stage union. A non-null verbatimSpan must match packet text exactly. REVIEW must name the precise ambiguity but still evaluates every declared stage. testedDecision, every basis, and reason must be item-specific; do not repeat stock text across rows.
