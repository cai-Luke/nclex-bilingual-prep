# Campaign 16 Phase E R1 — Stage 0/1 Execution Plan

Scope is limited to the frozen work order's Stage 0 and Stage 1 producer lane.

1. Freeze the live repository, index, work-order digest, and 13 bundled-bank byte hashes.
2. Run the five inherited trap dispositions, explicit all-bank validation, stage-reference tests, and explicit-file non-strict/strict measurements.
3. Rederive the live `revealsAllStages` identity population with the current executable audit, reconcile identities against Phase A and July orientation, and build fresh Phase-E-local packets.
4. Prove byte-identical packet determinism and exercise the candidate validator's valid control plus every required synthetic rejection.
5. Dispatch exactly one packet to each fresh Codex/GPT semantic context, validate each returned JSONL before acceptance, and allow at most one mechanical retry.
6. Concatenate validated outputs without semantic rewriting, freeze hashes, rederive all deterministic surfaces, and prove no path outside this producer root changed.

The checker root, Stage 2, Stage 3, bank repair, Phase F, Git/index mutation, and every unrelated repository mutation remain out of scope.
