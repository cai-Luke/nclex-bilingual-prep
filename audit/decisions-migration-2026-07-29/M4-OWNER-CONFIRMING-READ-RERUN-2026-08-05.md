# M4 `Owner` confirming-read rerun

**Date:** 2026-08-05  
**Seat:** Codex reading seat  
**Authorizing order:** `DECISIONS-MIGRATION-STAGE-2A-M4-OWNER-CONFIRMING-READ-RERUN-WORK-ORDER-2026-08-05.md`, revision 1

**Overall disposition: BLOCK — return to the architect seat for the in-scope `M4.4` reservation-recording conflict at §4.4 item 7.** The three commissioned `Owner` reasons each clear their own merits. This report is the sole write; no pre-existing manifest, M6, resume-note, or other repository byte was changed by this read.

## 1. Authorization and fresh identity checks

The closed void-receipt adjudication record was read before this §4 commission. The order itself was freshly measured at `28183` bytes with SHA-256 `53b7c0560f62f89e5cc8de288f076dd47e2c3485d12a11c5fcf054627b994bb6`: **MATCH** to revision 1's frozen authorization identity.

| artifact | fresh measurement | result |
|---|---|---|
| live manifest | `314491` bytes; `5943` physical lines; SHA-256 `877941d8af310567abe8c8510c1f551013faa0221f9c1dadf79d8feb98db4e46` | **MATCH** |
| revision-3 M4 anaphora repair order | `33084` bytes; `477` physical lines; SHA-256 `aadca7b586c795ac6e08ede55ab4dd06329ecfddee02965c1ea8a0a726df48ea` | **MATCH** |

The repair-order measurement supplies the remaining §1.2 digest obligation recorded in the void-receipt adjudication. The resume-note identity `65124` / `19f796d1…` was the authorized pre-edit null; it was not treated as a pin for the intentionally changed current resume note and is not a blocker.

## 2. M6 contact anchor

M6.1 was measured with Codex V3's published section boundary: from `### M6.1 Ground vocabulary` through line 5426's final content newline, excluding the separator blank line before M6.2. It measures `3579` bytes with SHA-256 `0216a4e02240a9c7017ba1819923234546643020245909c1290e1893d3a7212a`: **MATCH**.

The M6.3 rows below use V3's published row boundary: the visible table row from its opening pipe through its closing pipe, excluding the line-feed delimiter. No alternate boundary or replacement pin was derived.

## 3. `M4.3` / `P2#0`

### 3.1 Subject contact

Physical lines 390–393, ending immediately before the `Execution` backtick on line 393:

```text
`Owner` — `OMIT`; the review-scoping and self-certification limbs are carried by `AGENTS.md`'s
    risk-tiered verification floors, while the lane-declaration limb is carried by each active
    generation lane's own contract rather than by any central path, so no one tracked path carries
    the complete statement.
```

The quoted subject has one invisible trailing ASCII space after `statement.`, immediately before `` `Execution` ``. Its measurement is `319` bytes and SHA-256 `3d0ee0b9a949ac89643e81040506643257cc06679b4feedcb93612c5e9e7ffd6`: **MATCH**.

### 3.2 Required M6 pairing

M6.3 row 3, physical line 5518:

```text
| 3 | `E002` | `P2#0` | Owner | — | NO-SINGLE-OWNER | `OMIT` | M4.3 |
```

`71` bytes; SHA-256 `3eb9d83bd9a8d48ef8b923a4e67cbee567747fe36141f895ae0c03cb96ac22c2`: **MATCH**.

The assigned ground and the required adjacent ground, quoted from the M6.1 section measured above:

```text
| `NO-SINGLE-OWNER` | `Owner` only | Distinct tracked paths independently enforce distinct operative limbs, so no one path owns the rule. The one-path grammar admits no concatenation. |
```

```text
| `NO-EXECUTABLE-OWNER` | `Owner` only | The entry decides a practice, disposition, or scope boundary that no path executes. |
```

### 3.3 Verdict: CLEAR

1. **Self-containment:** The reason gives its own three-limb basis; it contains no cross-field anaphor or M6.1 ground token.
2. **Ground agreement and acceptance test:** It describes several tracked surfaces carrying different limbs and denies that any one carries the complete statement. It does not assert that no tracked path carries the rule, so it remains `NO-SINGLE-OWNER`, not `NO-EXECUTABLE-OWNER`.
3. **Live limb-to-surface read:** `AGENTS.md` is a current tracked enforcement surface: its risk-tiered table requires independent checking for judgment-dependent work and distinguishes bounded mechanical verification, including the committed-artifact census check. The current tracked evergreen prompt assigns GPT/Gemini as producer and Claude as the review/promotion gate. The current tracked direct-case contract is explicitly `active`, identifies its GPT producer, and requires a separate non-GPT checker. The dated 2026-07-18 evergreen note that no direct-case lane was then specified is not a present-tense contradiction of the 2026-07-19 contract's explicit active status. Even if the direct-case contract were discounted, `AGENTS.md` and the evergreen prompt remain separate surfaces for different limbs; the direct-case contract independently corroborates the per-lane routing claim.
4. **`Execution` adjacency:** The immutable `Execution` sentence asks whether a path executes the governance practice; the `Owner` reason identifies governance/enforcement surfaces and says none owns the whole rule. Those are different field tests, as the two M6.1 definitions show. The clauses are consistent; no return is triggered under §4.4 item 5.

## 4. `M4.7` / `P5#0`

### 4.1 Subject contact

Physical lines 611–615; the visible sentence ends on line 614 and the subject ends on line 615 immediately before the `Execution` backtick:

```text
`Owner` — `OMIT`; `BANK-REVIEW-LEDGER.md` records what the promotion pipeline did
    rather than owning the rule that governs it, and the self-review prohibition and the
    lane-declaration obligation bind before promotion runs, so they reach beyond anything the ledger
    or the promotion machinery enforces.
```

The quoted subject includes the newline after `enforces.` and four invisible ASCII spaces before `` `Execution` `` on line 615. Its measurement is `319` bytes and SHA-256 `e1d1ce93a0151ea3584318b761b53d88ae3aa33582e87426b932fa0802efb27c`: **MATCH**.

### 4.2 Required M6 pairing

M6.3 row 11, physical line 5526:

```text
| 11 | `E006` | `P5#0` | Owner | `BANK-REVIEW-LEDGER.md` | NOT-AN-AUTHORITY, PARTIAL-OWNERSHIP | `OMIT` | M4.7 |
```

`112` bytes; SHA-256 `3bf80a58c819a5c51ee87cdfe1b23d6c21b14b9cf75a88876b23852fc5be594c`: **MATCH**.

The two assigned M6.1 definitions, quoted from the M6.1 section measured above:

```text
| `NOT-AN-AUTHORITY` | both | The candidate is a tracked file, but is an implementation script, artifact, data file, or operational record rather than a source carrying evidence or a path owning the rule. |
```

```text
| `PARTIAL-OWNERSHIP` | `Owner` only | One candidate owns some operative limbs, but another operative limb has its own independent enforcement surface elsewhere. Standing rulings 11 and 28. A candidate is **not** disqualified merely because it cannot enforce a rationale, classification, or explanatory clause. |
```

### 4.3 Verdict: CLEAR

1. **Self-containment:** The reason does not rely on `Evidence`, another record, or an inherited phrase.
2. **Ground agreement and acceptance test:** It states both required limbs. `BANK-REVIEW-LEDGER.md` is an operational record of review/promotion status, as its own workflow and the repository runbook confirm; it does not own the governing rule. Separately, the self-review prohibition and per-lane declaration obligation apply before promotion, so they exceed the ledger and the promotion machinery's scope. This is the required authority limb plus partial-ownership limb, not a single-limb substitute.

## 5. `M4.11` / `P8#0`

### 5.1 Subject contact

Physical lines 838–840, ending immediately before the `Execution` backtick on line 840:

```text
`Owner` — `OMIT`; the upstream-ownership, read-only-downstream, and drop-rather-than-guess
    limbs attach at different stages of the authoring and transformation chain, and no single tracked
    path carries all three.
```

The quoted subject has one invisible trailing ASCII space after `three.`, immediately before `` `Execution` ``. Its measurement is `223` bytes and SHA-256 `eb6a3f8e33c188f3c0ecf7758c4289e5cd9b5da9c5cc285ca8f88d8e5190b4f5`: **MATCH**.

### 5.2 Required M6 pairing

M6.3 row 19, physical line 5534:

```text
| 19 | `E039a` | `P8#0` | Owner | — | NO-SINGLE-OWNER | `OMIT` | M4.11 |
```

`74` bytes; SHA-256 `85198fb91b7f828b41953db67d54422df1be037d9ece9b12a6e5f1a529bc7dda`: **MATCH**.

The assigned ground and required adjacent ground, quoted from the M6.1 section measured above:

```text
| `NO-SINGLE-OWNER` | `Owner` only | Distinct tracked paths independently enforce distinct operative limbs, so no one path owns the rule. The one-path grammar admits no concatenation. |
```

```text
| `NO-EXECUTABLE-OWNER` | `Owner` only | The entry decides a practice, disposition, or scope boundary that no path executes. |
```

### 5.3 Verdict: CLEAR

1. **Self-containment:** The reason states its own three limbs and has no cross-field reference or M6.1 ground token.
2. **Ground agreement and acceptance test:** It maps the statement's upstream-authoring, read-only downstream-transformation, and drop-rather-than-guess limbs to different stages, then states the one-path conclusion. It therefore expresses `NO-SINGLE-OWNER`; it does not claim an absence of all executing paths and does not drift into `NO-EXECUTABLE-OWNER`.

## 6. Ruling 35

**CLEAR.** The live ruling satisfies all six required limbs: its rule requires field-local reasoning where eligibility tests differ; it names the anaphor/allowlist mechanism; it states the byte-identity limitation and preserves the 2026-08-04 verification as correct within its commission; it expressly refuses to certify a surviving inherited reason; it makes search a candidate population governed by ruling 34; and it records the required 2026-08-05 GPT-confirming-read provenance. It supplies no worked counterexample.

## 7. `M4.4` reservation recording — BLOCK

This is a scope-recording finding only, not an adjudication of `M4.4` / `P2#1`.

The repair report and the current resume note's `Next session` block correctly state that `M4.4` is reserved and unadjudicated. The current resume note also says:

```text
**`M4.4` / `P2#1` is reserved, without adjudication, for the full constitutional review.** It carries the
same `same reason` construction, but no repair moved its antecedent, so it sits outside the causal surface
of the 2026-08-05 review return. It is neither cleared on the merits nor recorded as a defect, and no seat
may read the reservation as having settled it in either direction. Standing ruling 35 states why.
```

Immediately before that, on physical lines 140–143, it also says:

```text
M4 records authored — **all 65 live blocks**, inserted at M4.2–M4.66 in
`audit/decisions-migration-2026-07-29/target-text-manifest.md`. **All 65 live M4 records at M4.2–M4.66
are authored and provisionally cleared.**
```

That latter statement necessarily includes `M4.4`. It conflicts with the explicit S3 statement that `M4.4` is neither cleared on the merits nor recorded as a defect, and it makes false the required item-7 condition that `M4.4` is written nowhere as cleared. This is a recording inconsistency in an authorized resume-note surface; it does not decide the merits of `M4.4`. Return it to the architect seat for a further authorized resolution. No repair was made here.

## 8. Scope closure

The three commissioned subjects are **CLEAR**. The overall disposition remains **BLOCK** solely because §4.4 item 7's required reservation-recording condition is not met. No determination was made on the other sixty-two M4 records, including `M4.4`, or on any wrapper.
