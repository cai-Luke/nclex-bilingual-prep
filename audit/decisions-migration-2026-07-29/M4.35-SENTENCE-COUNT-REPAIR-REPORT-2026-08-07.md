# M4.35 sentence-count repair report

**Date:** 2026-08-07 · **Executing seat:** Architect (Claude) · **Status:** repair applied; Codex verification and confirming read OWED

## 1. Authorization

Work order: `DECISIONS-MIGRATION-STAGE-2A-M4.35-SENTENCE-COUNT-REPAIR-WORK-ORDER-2026-08-07.md`, revision 1, owner-measured and architect-acknowledged at **9177 bytes / SHA-256 `e54a2abff4d8499f10182562839143efd464ca5e5a5068c3faa7ff62b5c1693e`**, immutable from acknowledgment.

Per the two-hash rule, the closing measurement of this order is **OWED** and must be supplied by a hashing-capable seat. The architect seat has no SHA-256 primitive; it independently re-measured the order's byte length as `9177` before the first edit and reports that length only, not a digest.

## 2. Pre-repair identity

| item | value |
|---|---|
| Manifest byte length | `314811` (architect-measured) |
| Manifest SHA-256 | `e99335567d157a86f8f2b6f178b7222bf86ccfe15e5f827da5a03864c1d04b31` (carried from the prior pinned record; **not** independently re-measured by this seat) |
| Branch / HEAD | `codex/decisions-migration` / `05f9bcdddf9ce92f80bcd05fb5c4c5399fd466d5` |
| `DECISIONS.md` | `76314` bytes, untouched by this repair |

## 3. Anchor uniqueness, established before the edit

A repository-wide search for the anchor `about a leaf. Generation prompt parameters draw only from` returned exactly two hits: the manifest at line 2253, and `audit/decisions-migration-2026-07-29/TASK-2-TASK-3-REMEASUREMENT-2026-08-07.md` line 96, which is Codex's own report quoting the defective bytes and is not a repair target. **Within the manifest the anchor is unique.**

## 4. The edit

Dry run first, per standing discipline. Reported `313032 -> 313032` characters — length-preserving, as the order §2 predicted.

Applied live. Single substring, two byte substitutions:

- Before: `about a leaf. Generation prompt parameters draw only from`
- After: `about a leaf; generation prompt parameters draw only from`

`.` → `;` and `G` → `g`. No insertion, no deletion, no other substring touched.

## 5. Read-back from disk

A returned diff is not proof of persistence. The manifest was re-read after the write.

- Search for the post-repair anchor returns exactly one hit, manifest line 2253.
- Search for the pre-repair anchor returns zero hits in the manifest.
- `M4.35` item 8 reads, in full, the statement ending `…never standing as evidence about a leaf; generation prompt parameters draw only from this same scored-leaf population. Delivery and inventory reports measure what can be served… A \`case_study\` is a delivery container and may not enter equal-average scored-item-type targets unless a case-cadence target is separately ratified.`
- `M4.35` item 9 field lines are visually unchanged: `Kind: P`, `Status: ACTIVE`, `Force: BINDING`, `Date: 2026-07-16`, `Execution: EXECUTED`. **Byte-identity of item 9 is asserted by inspection only and remains for Codex to prove under order §4.2.**
- The exclusivity clause `draw only from this same scored-leaf population.` is present verbatim; only its leading `G` was recased.

## 6. Expected post-repair state — predictions, not measurements

Each of these is a prediction for Codex to test under order §4. None is a value to reconcile to.

1. Manifest byte length remains exactly `314811`. **The SHA-256 differs.** Byte length is worthless as a change proof for this repair; every check must be SHA-based, and a closeout reporting length alone is void.
2. `countStatementSentences` returns **3** for `M4.35`. Basis, read from source at `lib/decisions-format.ts` line 308: only `.`, `?`, and `!` are boundary candidates, so the semicolon contributes no boundary and the merged sentence yields one at its terminal period.
3. All 65 records return counts in `{1,2,3}`; no record other than `M4.35` changes from the 2026-08-07 run.
4. Item 9 of `M4.35` is byte-identical, leaving Task 3 discharged.
5. The other 64 live records and all 13 wrappers are byte-identical, preserving the fresh constitutional review for those 77 units.

## 7. Post-repair identity — OWED

The architect seat cannot hash. The post-repair manifest SHA-256 is **OWED** from a hashing-capable seat and must be measured afresh rather than derived. Until it is recorded, no seat may cite a post-repair manifest identity, and the derived date-occurrence report may not begin.

## 8. What this repair did not do

No other manifest byte was changed. `DECISIONS.md` was not opened for writing. No file was staged, committed, pushed, stashed, reset, checked out, or cleaned. No review receipt was edited. No Task 2 or Task 3 result was updated in place — Codex's `TASK-2-TASK-3-REMEASUREMENT-2026-08-07.md` stands as an accurate record of the state it examined, including its quotation of the now-superseded bytes.

## 9. Owed before the date-occurrence report

1. Post-repair manifest byte length and SHA-256, and the closing measurement of the work order, from a hashing-capable seat.
2. Codex deterministic verification under order §4 → `M4.35-SENTENCE-COUNT-REPAIR-VERIFICATION-2026-08-07.md`.
3. Non-Claude confirming read under order §5 → `M4.35-SENTENCE-COUNT-CONFIRMING-READ-2026-08-07.md`. **Barred to the Claude seat**, which authored both the original statement and this replacement wording.
4. Architect adjudication of both.
