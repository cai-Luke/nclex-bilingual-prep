{
  "checks": [
    {
      "name": "Browser archive abort preserves previous durable Last set and active record; idempotent Retry save succeeds"
    },
    {
      "name": "Learner hydration barrier disables session starts until durable progress, flags and uploads load"
    },
    {
      "name": "Real blocked DB v5→v6 upgrade is explicit, closes cleanly and recovers on reload"
    },
    {
      "name": "Built file://: bundled bank, durable submit, Last set archive and full reload rendering",
      "durableIndexedDB": true
    },
    {
      "name": "Built file:// with IndexedDB and localStorage denied remains usable and explicitly memory-only"
    }
  ]
}