# Review + Vocabulary omnibus R1 — implementation receipt

**Ready for producer-independent review; not independently certified.** No push, merge, deployment, or bank promotion was performed. No work was delegated.

- Worktree: `/Users/holemini/Desktop/Project Shrimp Review Vocab R1`
- Branch: `codex/review-vocab-omnibus-r1`
- Pinned source: clean `codex/ux-0c-finite-vocab-pass` at `bb29bff`
- Commits: `2d94da8` (baseline evidence and isolated collapsed-Chinese repair), `015b181` (omnibus implementation), `d7e7e0e` (migration-failure handling and acceptance coverage), `f046af5` (mandatory reveal-all and duplicate-Finish checks, consistent Needs-review labels), and `1329c9c` (compact Home utility links). The following evidence commit contains this receipt and generated verification artifacts.
- Governing inputs: the [copied brief](PROJECT-SHRIMP-REVIEW-VOCAB-OMNIBUS-BRIEF-2026-09-12.md) and [owner authorization/amendments](authorization.txt).

The learner now uses Study with Needs review, Saved, and Last set. Home carries the default-on inclusion control, case-aware Needs-review count, Saved link, and dated Last set. Library separates inspection from Practice; Customize offers All / Unanswered / Needs review / Saved. Progress reports factual attempts and coverage. Completion and reopened Last set share the same viewer, with stored outcomes, historical scopes, explicit unsubmitted entries, and fresh retry actions.

**Deletion inventory.** Removed Vocabulary/flashcard passes and term SRS; question SRS and Due UI; Rescue launches and English-miss marking; language-miss, translation-reveal, and case-part telemetry; related-practice scoring and its only-use `seedFromString`; retired controls, metrics, CSS, types, storage APIs, and tests. Also removed the now-unused unconditional active-clear API. There is no question snapshot store, historical renderer version system, retry graph, Saved Terms replacement, or compatibility layer for retired modes. Contextual glossary help, pronunciation, bilingual reveal, Copy-for-GPT, ordinary answerEvents, active-session protection, grading, and deterministic clinical rendering survive.

**Storage and migration.** DB v6 owns `uploadedQuestions`, `progress`, `flags`, `answerEvents`, `activeSession`, and `completedSets` (one record keyed `last`); settings remain in localStorage. Progress uses one `needsReview` boolean. Below-full-marks commits set it, and one later full-marks commit clears it, including whole-case aggregate outcomes. Saved remains the existing manual flags boolean with note/timestamp intact. A note-only row remains unsaved.

The v6 upgrade clears valid legacy residual misses after one correct attempt, retains counters/timestamps, and resolves ambiguous rows only from a consistent, timing-supported latest top-level event; otherwise it preserves the old boolean and records a diagnostic. No lifetime-incorrect inference or case-part-event inference is used. Legacy active work remains preserved and explicitly unverified; missing IDs and invalid draft IDs show recovery rather than disappearing. An injected migration write failure rolls the entire upgrade back to intact v5 data, then succeeds on retry.

**Submission and completion durability.** The pre-fix browser control produced **2 durable answerEvents** from two clicks under 300 ms delayed progress reads, despite `seen = 1` ([baseline](baseline.json), [negative control](negative-control.json)). The final browser triple-click test with delayed real IndexedDB transactions produces **1 durable event and 1 attempt**, surviving reload. Delayed triple Finish also converges on one archive and clears only the matching active record. A synchronous submit lock prevents edits during I/O; the unique session/question identity and atomic progress + event + captured-answer + active-set transaction independently deduplicate retries. Persistence reports durable versus memory outcomes.

Completion uses an ordered transaction to archive and clear only the matching active set. Failure preserves the previous durable archive and resumable work, exposes visit-only status, and offers idempotent retry. **Remediation cannot overwrite Last set**: Needs review, Saved, Library/explicit populations, and Last-set retries carry remediation intent. Zero-submission exits also leave the archive unchanged.

**Selection contract.** Previously, unseen items could indefinitely suppress misses within a category. The amended reservation now runs first: off/empty/N<5 gives zero, otherwise `min(pool, max(1, floor(N/5)))`, globally by oldest attempt with category borrowing. Non-remediation seats retain ordinary NCLEX weighting, documented by the narrow P10 application. Visual floors retain their existing minimum-tier rule and skip a kind already covered by a reservation; ordinary tier-1 exhaustion backfill and the final shuffle remain. Toggle off excludes misses entirely. Explicit populations never backfill.

**Verification.** [Command results and source hashes](verification-results.json), [browser results](browser-results.json), and [lifecycle/file results](lifecycle-browser-results.json) record passing checks. All-bank validation, aggregate audit (existing advisory warnings), census check without regeneration, the full visual regression aggregate, focused grading/schema/session/Copy/migration tests, decisions-format checks, typecheck, build, and diff check passed. Banks, schema validator, grader, import/loading code, and visual modules are unchanged. The fingerprint test covers response identity/content and context changes while excluding teaching edits. A [throwing-grader browser control](historical-grader-proof.json) opens all nine item types without regrading history.

Chrome checks cover HTTP and the built `file://` app with durable reload, denied storage, failed archive writes, a genuinely blocked upgrade, missing/damaged active recovery, keyboard replacement and Saved-removal focus, read-only matrix controls, and desktop/390×844 layouts in both themes at 100/125/150/200% text. **Chinese on-tap and reveal-all behavior survived telemetry deletion; explicit glossary help works with ambient Chinese Off.** Copy-for-GPT was exercised from the actual question. The collapsed Summary defect was independently reproduced and fixed. Screenshots are included in this packet.

**Remaining boundary.** DB v7 physical deletion of the four obsolete stores and inert legacy progress columns is intentionally deferred until independent acceptance of v6 migration, submission idempotency, and Last-set persistence. PROJECT-HISTORY publication is deferred to the post-review publishing seat. No deviations from the owner-amended implementation contract are claimed; independent conformance remains pending.

`REVIEW_VOCAB_OMNIBUS_R1_READY_FOR_INDEPENDENT_REVIEW`
