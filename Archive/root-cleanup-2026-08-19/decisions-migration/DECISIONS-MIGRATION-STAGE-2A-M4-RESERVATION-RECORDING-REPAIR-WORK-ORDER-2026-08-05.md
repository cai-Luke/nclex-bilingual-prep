# Stage 2a — M4.4 reservation-recording repair work order

**Date:** 2026-08-05 · **Authoring seat:** Architect · **Revision:** 1

**Class: review return, resume-note only.** This order is not a continuation of
`DECISIONS-MIGRATION-STAGE-2A-M4-OWNER-CONFIRMING-READ-RERUN-WORK-ORDER-2026-08-05.md` revision 1.
Architect editing under that order is closed and stays closed, and its two-hash proof is discharged at
`M4-OWNER-CONFIRMING-READ-VOID-RECEIPT-ADJUDICATION-2026-08-05.md` §7, which is **CLOSED and not
reopened by this order**. Every edit authorized below is authorized by **this** order and by no other.

**It opens no manifest byte and no ruling.**

**Origin.** The Codex reading seat returned
`audit/decisions-migration-2026-07-29/M4-OWNER-CONFIRMING-READ-RERUN-2026-08-05.md`. The three
commissioned `Owner` reasons at `M4.3` / `P2#0`, `M4.7` / `P5#0`, and `M4.11` / `P8#0` are each **CLEAR**
on proof of contact, M6 pairing, ground agreement, the §5.3 acceptance tests, the `M4.3` live
limb-to-surface read, the `Execution` adjacency, and ruling 35. Every pin re-measured **MATCH**. The
overall disposition is **BLOCK**, on §4.4 item 7 alone: the resume note simultaneously reserves `M4.4` /
`P2#1` without adjudication and sweeps it into blanket clearance statements.

**The block is accepted. The three CLEAR verdicts are accepted.** No further semantic review of those
three reasons is commissioned, by any seat, under this order or a later one.

---

## 0. Revision history

**Revision 1 — this revision.** No prior revision exists. **No byte length or SHA-256 has been taken, and
none may be inferred.** This order is a draft with no authorization identity until §1.3 is satisfied.

---

## 1. Identity, authorization, and the bar on editing

### 1.1 Repository state, pinned

| item | value |
|---|---|
| Repository | `Project Shrimp`, local worktree |
| Branch | `codex/decisions-migration` |
| HEAD | `05f9bcdddf9ce92f80bcd05fb5c4c5399fd466d5` |
| `MIGRATION_BASELINE` | `d499cc1d0916e03830489ec9cd0324cd1a203a73` |
| `DECISIONS.md` | `76314` / `b22b3fffc058a5e84931ca1b14b08a1b7f1206d1ccd5154a7a7f1de8ad0f0d6e`, byte-identical to baseline |

### 1.2 Artifact identities

| path | bytes | SHA-256 | status |
|---|---:|---|---|
| `audit/decisions-migration-2026-07-29/target-text-manifest.md` | `314491` | `877941d8af310567abe8c8510c1f551013faa0221f9c1dadf79d8feb98db4e46` | pinned, **closed**, re-measured at §7 |
| `DECISIONS-MIGRATION-STAGE-2A-CLAUDE-RESUME-NOTE-2026-07-29.md` | `71861` | **owed from a hashing seat before the first edit** | the sole mutable artifact |

**The resume-note digest is a hard prerequisite.** `71861` is an architect-seat length measurement only.
The resume note is the one artifact this order authorizes this seat to mutate, its pre-edit digest is the
null against which any later reversal proof runs, and that null ceases to exist the moment the first edit
lands. A length match is consistent with an equal-length edit and is not a substitute. Execution does not
begin until the digest is recorded at §8 §1.

The three `.frozen` snapshots stay untouched at `312411`, `308092`, and `55424`.

### 1.3 The authorization identity

The architect seat has no hashing primitive. **No edit authorized by this order may be made until a seat
that can hash has returned this file's byte length and SHA-256 at revision 1, and those values are
transcribed into the record at §8.** The same identity is re-measured after the last edit; opening and
closing values must be equal, both are measurements, and neither is transcribed from the other.
Inequality is a BLOCKER under §9.

**This order is immutable from the moment its revision-1 identity is taken.**

---

## 2. The defect, and the population

`M4.4` / `P2#1` is authored, reserved, and unadjudicated. The resume note says so explicitly in the
`## Next session` block. It also says, in three other places, that every M4 record is cleared. Both cannot
be true, and the conflict makes false the §4.4 item 7 condition that `M4.4` is written nowhere as cleared.

**This order decides nothing about `M4.4`'s merits.** It is a recording repair. The reservation is
untouched and remains routed to the full 65-record, 13-wrapper constitutional review.

### 2.1 The retained population — three statements

| # | location | text, as it currently reads | why it is a finding |
|---:|---|---|---|
| 1 | resume note, `## Cursor`, physical lines 141–142 | **All 65 live M4 records at M4.2–M4.66 are authored and provisionally cleared.** | `M4.2–M4.66` contains `M4.4` by construction |
| 2 | resume note, physical line 414 | **Part A is provisionally closed.** | Part A's assigned range is `P1#0` through `P17#0` — M4.2–M4.19 — and `P2#1` is `M4.4`. *Closed* is not *cleared* on its face, but the line sits among batch-disposition statements and reads as a review status; an ambiguous status line over a reserved record is itself the defect |
| 3 | resume note, `## Review status`, physical line 952 | All sixty-five M4 records at M4.2–M4.66 carry provisional non-author clearance. | same construction as 1 |

### 2.2 The excluded population, with a ground for every exclusion

Ruling 34 governs: the sweep returned a candidate population, and every exclusion carries a stated ground.
The candidate sweep ran over `provisionally cleared`, `provisional non-author clearance`,
`provisionally closed`, `provisionally reviewed`, `carry provisional`, `all 65`, `All 65`, `sixty-five`,
and `Part A`.

| location | ground for exclusion |
|---|---|
| resume note lines 165, 170, 174, 178, 224, 228, 270, 338 — the batch statements | **range.** Every one ranges over M4.20–M4.66. `M4.4` falls outside all of them |
| line 140, `M4 records authored — all 65 live blocks` | **content.** An authorship claim, which stays true and is required to stay true |
| line 160 and its Part A block list, which includes `P2#1` | **content.** An authorship inventory. It asserts no clearance |
| lines 369 and 493 | **content.** Both describe the full review as *owed*, which is the correct state |
| line 780 | **content.** Inside ruling 34, describing a search population. Rulings are closed under §3.2 |
| line 883, `no provisionally cleared batch is reopened` | **content and surface.** It asserts non-reopening, not clearance of any named record, and it sits inside the record of an owner act |

**Two seats reached this population independently.** The Codex receipt quoted statement 1. The GPT
adjudication named all three. This seat swept the note and retained the same three, with the grounds
above. No fourth statement was found.

---

## 3. Scope

### 3.1 Open

- The six resume-note surfaces at §5.
- One new architect-written repair record at §8.
- One Codex confirmation deliverable at §7.

### 3.2 Closed — a change to any of these is a BLOCKER

- **The entire manifest**, all `314491` bytes. No manifest edit is authorized under this order for any
  reason, including one a finding appears to require.
- `audit/decisions-migration-2026-07-29/M4-OWNER-CONFIRMING-READ-RERUN-2026-08-05.md`, the Codex receipt.
- **Standing rulings 35 and 36, and rulings 1–34** — every ruling byte, verbatim.
- `M4-OWNER-ANAPHORA-REPAIR-REPORT-2026-08-05.md` and `M6-REPAIR-REPORT-2026-08-04.md`, both in full,
  including the former's stale §7.
- `M4-OWNER-CONFIRMING-READ-VOID-RECEIPT-ADJUDICATION-2026-08-05.md`, which is **CLOSED**. The Codex
  receipt's §1 supplies the revision-3 digest that record listed as owed at its §1.1; that discharge is
  recorded at §8 of **this** order's repair record, not by reopening a closed file.
- Every prior work order, verification, review file, and receipt.
- The three `.frozen` snapshots.
- `DECISIONS.md`.
- Every resume-note byte outside the six surfaces at §5.

### 3.3 `M4.4` / `P2#1`

Reserved, unadjudicated, unchanged. **This order takes no position on the merits and no seat may read it
as having taken one.** The repair makes the note's recording consistent with the reservation; it does not
convert the reservation into a finding, and it does not convert it into a clearance.

---

## 4. The required resulting status

Every repaired surface states all three limbs, or states one limb consistently with the other two:

1. **All 65 M4 records at M4.2–M4.66 are authored.**
2. **Sixty-four carry provisional non-author clearance.**
3. **`M4.4` / `P2#1` is the sole exception: authored, reserved, unadjudicated, and not cleared** — routed
   to the full 65-record, 13-wrapper constitutional review.

The count `64` is derived as 65 minus the single reserved record and is stated as such, so a later seat
can check the arithmetic rather than trust the number. **No surface may state limb 2 without limb 3 on the
same surface.** Separating them is how the present defect arose: the reservation and the blanket claim
were each individually defensible and sat forty lines apart.

---

## 5. Resume-note allowlist — exactly six surfaces

| id | surface | change |
|---|---|---|
| S1 | `## Cursor`, the statement at physical lines 141–142 | restate per §4. The adjacent authorship sentence at line 140 is retained unchanged |
| S2 | physical line 414, `**Part A is provisionally closed.**` | restate so Part A's status names `M4.4` as the reserved exception. The range description — `P1#0` through `P17#0`, 13 cores and 5 attachments — is factual and retained |
| S3 | `## Review status`, the statement at physical line 952 | restate per §4. The following sentences recording which batches cleared on which dates are factual and retained |
| S4 | `## Next session — start here`, the next-action and confirming-read paragraphs | record the Codex receipt: three subjects **CLEAR**, all pins **MATCH**, overall **BLOCK** on the reservation-recording defect alone; name this order as the repair and §7 as the next action. The void-receipt paragraph, the revision-3 discharge paragraph, the routing list, the schedule constraint, the delegation clauses, the hashing-primitive note, and the write-back note are all retained |
| S5 | `## Cursor`, the two spans edited as S4a and S4b under the rerun order | record the confirming read as returned with the three reasons cleared and the commission blocked on recording only. Everything from `Prior context,` onward is immutable |
| S6 | the `**Updated:**` line only | set to the landing date in `America/New_York`; unchanged if equal to the value present. `**Seat:**` is immutable |

**The `M4.4` reservation paragraph in the `## Next session` block is not opened.** It is already correct,
and it is the surface the other three contradicted. Editing it would be repairing the wrong end of the
conflict.

---

## 6. No new standing ruling

**No standing ruling is authorized under this order.** The note must carry `34.`, `35.`, `36.` and no
`37.` at closeout, and rulings 1–36 must be byte-identical.

Recorded so the omission is legible as a decision rather than an oversight: the generalizable lesson here
— that a reservation recorded in one place and contradicted in three is a reservation that has not been
recorded — is already inside ruling 35's limit limb, which requires an unchanged anaphor to be left *owed
an adjudication rather than settled in either direction*. The defect is that limb going unenforced across
the rest of the document, not a gap in the rule. A ruling restating an existing rule dilutes the list that
seats actually read.

---

## 7. Codex confirmation commission

Executed after the repair record at §8 exists. **Seat:** Codex. One deliverable:
`audit/decisions-migration-2026-07-29/M4-RESERVATION-RECORDING-CONFIRMATION-2026-08-05.md`. That file is
the **sole authorized write**. No repair, staging, commit, push, stash, reset, clean, or checkout.

**Scope is the recording only.** The semantic review of `M4.3`, `M4.7`, and `M4.11` is **discharged** by
the 2026-08-05 rerun receipt and is **not** repeated, revisited, or re-verified. A receipt that re-argues
those three reasons has exceeded its commission.

| # | check | disposition |
|---:|---|---|
| C1 | re-measure the manifest: `314491` / `877941d8…`, unchanged | MATCH / MISMATCH |
| C2 | re-measure this order at revision 1 against the identity at §8 §1, at opening and at close | MATCH / MISMATCH |
| C3 | **enumerate independently** every statement in the resume note asserting clearance, closure, or review status over a record range that contains `M4.4`, and report the enumeration as a population before adjudicating it. Do not take §2.1's three as the population | PASS / FAIL |
| C4 | each statement in that population states all three §4 limbs, or states one consistently with the others; no surface states limb 2 without limb 3 | PASS / FAIL |
| C5 | `M4.4` / `P2#1` is written nowhere in the note as cleared, closed, or reviewed, and the §4.4 item 7 condition now holds | PASS / FAIL |
| C6 | the reservation is written nowhere as a defect or as adjudicated, in either direction | PASS / FAIL |
| C7 | rulings 1–36 byte-identical; the note carries `34.`, `35.`, `36.` and no `37.` | PASS / FAIL |
| C8 | reversing S1–S6 reproduces the pre-repair note byte-for-byte against the §1.2 digest; measured byte length, line count, and SHA-256 reported | PASS / FAIL |
| C9 | no byte changed outside the six allowlisted surfaces; `Prior context,` onward byte-identical | PASS / FAIL |
| C10 | repository boundary: `DECISIONS.md` byte-identical to baseline; branch and HEAD unchanged; `git status --porcelain` shows untracked Stage 2a paths only, zero staged, zero modified tracked | PASS / FAIL |

**C3 is the check that matters and it is deliberately not a lookup.** This order's §2.1 is the architect
seat's own enumeration, and a confirmation that adopts it has verified nothing about whether the sweep was
complete. Enumerate first, then compare, then report any statement this order missed as a finding.

Findings beyond C1–C10 are ADVISORY, name their surface, and authorize no edit.

---

## 8. Repair record

The architect seat writes exactly one new file:
`audit/decisions-migration-2026-07-29/M4-RESERVATION-RECORDING-REPAIR-REPORT-2026-08-05.md`, carrying:
§1 this order's revision-1 identity, measured before the first edit, plus the resume-note pre-edit digest
required at §1.2; §2 the six surfaces as read back from disk; §3 the retained and excluded populations as
applied, with grounds; §4 the discharge of the void-receipt record's outstanding revision-3 digest
obligation by the Codex receipt's §1 measurement, recorded here because that record is closed; §5
architect-measured post-repair state, labelled as the producer's self-report; §6 the closing
authorization measurement.

**§6 is filled in place at closeout and this record is not marked closed until it carries a
measurement.** Every architect-produced figure discharges nothing. **A write is not evidence of itself:**
every edit is read back from disk before it is reported as applied.

---

## 9. Blockers

Execution stops and returns to the architect seat on any of: a §1.2 identity mismatch; a missing
resume-note pre-edit digest; an inequality between the opening and closing measurements of this order;
**any manifest edit whatsoever**; any change to a §3.2 surface; a change to any ruling byte; a C3
enumeration returning a statement outside §2.1; or any finding that would require adjudicating `M4.4` on
the merits.

**Deadline context, not authority.** `MIGRATION_DATE` is bound to `2026-08-11`. As of 2026-08-05 this
repair, its confirmation, the derived date-occurrence report, the Task 2 rerun and Task 3 rerun or
supersession, the post-assembly deterministic verification, the full 65-record and 13-wrapper non-author
constitutional review, and owner ratification are all owed against six days. **Schedule pressure
authorizes nothing.** This defect is the cheap kind — a recording inconsistency caught before
ratification. The expensive kind is a ratified constitution asserting that a record was reviewed when no
seat ever reviewed it.
