# DECISIONS migration Stage 2a — GPT review of Part B architect draft

**Date:** 2026-07-29  
**Status:** Independent provisional review. Not manifest ratification and not Stage 2b authorization.

> **HISTORICAL REVIEW ARTIFACT — NOT A CURRENT VERDICT.** Banner added 2026-07-30. This review is
> preserved unchanged as the record of what one seat saw on 2026-07-29. It is superseded as a clearance
> statement and must not be cited as one.
>
> **What it missed.** It accepted `P23#0` while that candidate dropped four live limbs: the per-part
> submit and true unfolding reveal deferral with its named revisit condition; `flags` and `adaptive`
> from the list of surfaces keying on the top-level question id; and the always-visible global exhibits
> from stage visibility. All four were found later and restored in the manifest.
>
> **What it got right but incompletely.** Its `P21#0` repair correctly demanded the enumerated semantic
> floor, but the resulting wording merged clinical scope and monitorability into one limb. That was
> refined later, and the manifest carries them separately.
>
> **Standing status of the rest.** Its `P24#0` and `P25#0`–`P25#3` findings are inputs for fresh review,
> not clearance. A later seat must re-derive them against the live source rather than inheriting this
> document's dispositions.

## Result

The Part B structure, block order, attachment topology, dates, classifications, and directed P25/P31 split are accepted. Four semantic repairs and one optional-field omission should be applied before Part C proceeds. These are bounded corrections, not a change of migration strategy.

## Accepted calls

- F1's `Evidence` and `Owner` fields are grammar-fixture examples, not ratified target fields. Keep both omitted from P25#0.
- Keep P26#0 `Evidence` omitted; the extraction proposal supports the forcing origin rather than the full generalized principle.
- P25#1 correctly carries the general chart-plus-derived-table rule without superseded multi-panel geometry.
- P25#2 correctly owns current unified-chart geometry.
- P25#3 correctly remains `P | ACTIVE | BINDING | Execution: PENDING`.
- P31 correctly removes retired P8/P18/P22 lane topology while preserving raw-volume-only generation, no direct canonical edits, no content-judgment audit role, and pair-specific EN+ZH reconciliation.

## Required repairs

### 1. P19#0 — omit `Owner`

`src/schema.ts` owns the shared projection and substantial validation behavior, but it is not the singular executable owner of the entire statement, which also governs the rationale-visual teaching slot and the continuing load-bearing rule for `question.visual`. Under the ratified conservative owner policy, omit the line rather than narrow the statement to fit one file.

### 2. P21#0 — restore the actual semantic floor

The current statement says only that the prompt inlines a semantic-quality floor, without stating what binds or pointing to one complete evidence/owner path. Preserve the source's operative floor in compressed form: no filler distractors; rationales for keyed and non-keyed choices; closed-world and monitorable stems; no lazy provider-notification key; unique ordered-response sequences; bounded highlight selection; gradeable closed-vocabulary blanks; bilingual parity; and failure on a nonexistent keyed ID.

### 3. P23#1 — restore the `lab_trend` allocation limb

The source application contains two independently binding shape allocations. The draft retains only structured measurements. Restore that the measured one-series `lab_trend` takes the full-width route while the two-series shape remains split, alongside the independent structured-measurement density predicate.

### 4. P24#0 — restore the canonical-boundary limb

Preserve that non-rendering migration dispositions such as serial skips, empty extracts, excluded values, and unit aliases remain ledger/staging-only and never enter canonical banks. This is a binding current rule, not evidence or chronology.

### 5. P25#0 — preserve the non-qualifying criterion and reversal rule

The current draft preserves the two primary fences but drops two operative consequences: vendor ubiquity is not a necessity criterion, and repeated single-timepoint-answerability failures close the waived kind to new content until the collapse gate holds. Compress these into the statement; do not add F1's illustrative optional fields.

## Part C boundary reminders

- R order is R1 through R6, regardless of source-packet order.
- E043a contains only the live `opus*` deterministic routing rule and the `claude_*` exclusion. Exclude the replacement direct-GPT contract, rerun assessment, case-form advice, and general P5 review obligations.
- R3/E047c carries the copied-renderer-envelope finding, the ratified 46.5 °C temperature ceiling, and `EXECUTED`; it excludes the temperature floor and every other vital side.
- R5/E047a carries SBP ceiling 400 mmHg, RR ceiling 150/min, SpO2 floor 0%, the bedside/charted population, unchanged renderer envelopes, and the fresh-survey prerequisite; `Execution: PENDING`. Exclude DBP, MAP, temperature floor, and laboratory SaO2.
- E047b carries only unresolved DBP/MAP ceiling sourcing, temperature floor, laboratory SaO2, and other unratified sides.
- E038 must state the durable July 28 operational-state rule and must not preserve GPT-5.6 as timeless constitutional text.
